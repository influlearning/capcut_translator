import { defineConfig } from 'vite';

export default defineConfig({
  // GitHub Pages: https://influlearning.github.io/capcut_translator/
  base: '/capcut_translator/',
  build: {
    outDir: 'dist',
  },
});
